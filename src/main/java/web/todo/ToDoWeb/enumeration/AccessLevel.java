package web.todo.ToDoWeb.enumeration;

public enum AccessLevel {
    /**
     * PRIVATE: Only user can see users todos
     * PROTECTED: Only user and followers can see users todos
     * PUBLIC: Everyone can see users todos
     */
    PRIVATE, PROTECTED, PUBLIC
}
